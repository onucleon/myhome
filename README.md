# FARMERZ — EN/NL Clean Setup (Fixed)

Next.js 14 App Router + Tailwind + EN/NL routes + client-side cart. Vercel-ready.

## Local
npm install
npm run dev

## Deploy
- Upload this folder to a GitHub repo (e.g., onucleon/myhome)
- Vercel → New Project → Import from GitHub → Deploy
- Live URLs: /en and /nl (root / redirects based on browser language)
